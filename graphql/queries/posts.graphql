query Posts($limit: Int!, $cursor: Int) {
  posts(limit: $limit, cursor: $cursor) {
    posts {
      id
      text
      title
      points
      createdAt
      updatedAt
      user {
        id
        username
      }
    }
    hasMore
  }
}